# Production backend URL — used by EAS builds (development, preview, production profiles).
# This file IS committed to git. It only contains the public API URL, which is not sensitive.
EXPO_PUBLIC_API_URL=https://terraform-backend.vercel.app
